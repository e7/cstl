#! /bin/bash

if [ ! -x "m4" ]; then
    mkdir m4
fi

aclocal &&
autoheader &&
libtoolize &&
autoconf &&
automake --add-missing &&

./configure &&
make CFLAGS="-Wall -Werror -std=c99" LDFLAGS="-L${PWD}/src/.libs"
